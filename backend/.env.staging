# Firestore Database
export FIRESTORE_DATABASE=staging

# Staging URLs
export GOOGLE_REDIRECT_URI=https://vtuber-analyzer-staging.web.app/oauth/callback
export FRONTEND_BASE_URL=https://vtuber-analyzer-staging.web.app
export ALLOWED_ORIGINS=https://vtuber-analyzer-staging.web.app,http://localhost:5173

# WebSub Callback (需在部署後更新為實際 Cloud Run URL)
export WEBSUB_CALLBACK_URL=https://youtube-api-staging-service-xxxxx-de.a.run.app/websub-callback

# Debug Mode
export OAUTH_DEBUG_MODE=true
