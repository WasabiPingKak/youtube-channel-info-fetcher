name: Deploy Production

on:
  push:
    branches: [main]

concurrency:
  group: deploy-production
  cancel-in-progress: false

env:
  GCP_PROJECT_ID: vtuber-channel-analyzer-v3
  GCP_REGION: asia-east1

jobs:
  # ── 品質閘門 ──────────────────────────────────────
  backend-check:
    name: Backend Lint & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Ruff check
        run: ruff check . --config ruff.toml

      - name: Ruff format check
        run: ruff format . --config ruff.toml --check

      - name: Run pytest
        run: pytest --tb=short -q
        env:
          JWT_SECRET: ci-test-secret
          ADMIN_API_KEY: ci-test-admin-key
          ADMIN_CHANNEL_IDS: UC_test_channel
          ECPAY_HASH_KEY: test_hash_key_1234567
          ECPAY_HASH_IV: test_hash_iv_12345
          ECPAY_MERCHANT_ID: "2000132"

  frontend-check:
    name: Frontend Lint & Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend_react
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend_react/package-lock.json

      - run: npm ci

      - name: ESLint
        run: npm run lint

      - name: Build (production)
        run: npm run build -- --mode production
        env:
          VITE_FIREBASE_API_KEY: ${{ vars.PROD_VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ vars.PROD_VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ vars.PROD_VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ vars.PROD_VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ vars.PROD_VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ vars.PROD_VITE_FIREBASE_APP_ID }}
          VITE_FIREBASE_MEASUREMENT_ID: ${{ vars.PROD_VITE_FIREBASE_MEASUREMENT_ID }}
          VITE_GOOGLE_CLIENT_ID: ${{ vars.PROD_GOOGLE_CLIENT_ID }}
          VITE_GOOGLE_REDIRECT_URI: ${{ vars.PROD_GOOGLE_REDIRECT_URI }}
          VITE_API_BASE: ""
          VITE_ENABLE_SETTINGS: "false"
          VITE_ADMIN_CHANNEL_ID: ${{ vars.PROD_INPUT_CHANNEL }}
          VITE_GA_MEASUREMENT_ID: G-Q2S0XSEW74

      - uses: actions/upload-artifact@v7
        with:
          name: frontend-prod-dist
          path: frontend_react/dist
          retention-days: 1

  # ── 部署 Backend ──────────────────────────────────
  deploy-backend:
    name: Deploy Backend (Production)
    needs: [backend-check]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v6

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Write version.txt
        run: echo "$(git rev-parse --short=6 HEAD)" > version.txt

      - name: Build container image
        run: |
          IMAGE="gcr.io/${{ env.GCP_PROJECT_ID }}/youtube-api-service:latest"
          BUILD_ID=$(gcloud builds submit --tag "$IMAGE" --async --format='value(id)' --quiet .)
          echo "BUILD_ID=$BUILD_ID" >> "$GITHUB_ENV"
          echo "IMAGE=$IMAGE" >> "$GITHUB_ENV"
          echo "⏳ Build submitted: $BUILD_ID"

          while true; do
            STATUS=$(gcloud builds describe "$BUILD_ID" --format='value(status)' 2>/dev/null)
            if [ "$STATUS" = "SUCCESS" ]; then
              echo "✅ Build succeeded"
              break
            elif [ "$STATUS" = "FAILURE" ] || [ "$STATUS" = "CANCELLED" ] || [ "$STATUS" = "TIMEOUT" ]; then
              echo "❌ Build failed: $STATUS"
              gcloud builds describe "$BUILD_ID" --format='value(statusDetail)' 2>/dev/null || true
              exit 1
            fi
            sleep 10
          done

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy youtube-api-service \
            --image "${{ env.IMAGE }}" \
            --region "${{ env.GCP_REGION }}" \
            --max-instances=3 \
            --allow-unauthenticated \
            --update-secrets "\
          JWT_SECRET=youtube-api-jwt-secret:latest,\
          API_KEY=youtube-api-api-key:latest,\
          GOOGLE_CLIENT_SECRET=youtube-api-google-client-secret:latest,\
          ADMIN_API_KEY=youtube-api-admin-api-key:latest,\
          ADMIN_CHANNEL_IDS=ADMIN_CHANNEL_IDS:latest,\
          ECPAY_MERCHANT_ID=ecpay-merchant-id:latest,\
          ECPAY_HASH_KEY=ecpay-hash-key:latest,\
          ECPAY_HASH_IV=ecpay-hash-iv:latest" \
            --set-env-vars "^@@^INPUT_CHANNEL=${{ vars.PROD_INPUT_CHANNEL }}@@GOOGLE_CLOUD_PROJECT=${{ env.GCP_PROJECT_ID }}@@GOOGLE_CLIENT_ID=${{ vars.PROD_GOOGLE_CLIENT_ID }}@@GOOGLE_REDIRECT_URI=${{ vars.PROD_GOOGLE_REDIRECT_URI }}@@FRONTEND_BASE_URL=${{ vars.PROD_FRONTEND_BASE_URL }}@@ALLOWED_ORIGINS=${{ vars.PROD_ALLOWED_ORIGINS }}@@WEBSUB_CALLBACK_URL=${{ vars.PROD_WEBSUB_CALLBACK_URL }}@@FIRESTORE_DATABASE=${{ vars.PROD_FIRESTORE_DATABASE }}@@GAME_ALIAS_ENDPOINT=${{ vars.PROD_GAME_ALIAS_ENDPOINT }}@@CLOUD_TASKS_LOCATION=${{ env.GCP_REGION }}@@CLOUD_TASKS_QUEUE=websub-subscribe@@CLOUD_RUN_SERVICE_URL=${{ vars.PROD_CLOUD_RUN_SERVICE_URL }}@@KMS_KEY_RING=vtmap-keyring@@KMS_KEY_ID=refresh-token-key" \
            --quiet

      - name: Route traffic to latest
        run: |
          gcloud run services update-traffic youtube-api-service \
            --to-latest \
            --region "${{ env.GCP_REGION }}" \
            --quiet

  # ── 部署 Frontend ─────────────────────────────────
  deploy-frontend:
    name: Deploy Frontend (Production)
    needs: [frontend-check]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend_react
    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v8
        with:
          name: frontend-prod-dist
          path: frontend_react/dist

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Deploy to Firebase Hosting
        run: firebase deploy --only hosting:prod --project "${{ env.GCP_PROJECT_ID }}"
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
